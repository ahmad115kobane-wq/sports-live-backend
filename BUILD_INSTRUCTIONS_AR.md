# تعليمات بناء التطبيق للأندرويد 📱

## المتطلبات الأساسية ✅
- ✅ تم تثبيت EAS CLI
- ✅ حساب Expo (مجاني)
- ✅ اتصال بالإنترنت

---

## الطريقة 1: البناء على السحابة (موصى به) ☁️

### خطوة 1: تسجيل الدخول إلى Expo
```bash
cd mobile
eas login
```

### خطوة 2: إعداد المشروع (أول مرة فقط)
```bash
eas build:configure
```

### خطوة 3: بناء APK للتجربة
```bash
eas build --platform android --profile preview
```

**أو** بناء للإنتاج:
```bash
eas build --platform android --profile production
```

### خطوة 4: تحميل APK
- بعد اكتمال البناء، ستحصل على رابط لتحميل APK
- حمّل الملف وثبته على هاتفك

---

## الطريقة 2: البناء المحلي (يتطلب Android Studio) 💻

### المتطلبات:
- Android Studio مثبت
- Android SDK
- Java JDK

### الخطوات:
```bash
cd mobile
npx expo run:android
```

---

## ملفات البناء المتاحة 📦

### Preview Build (للتجربة)
- **النوع**: APK
- **الحجم**: أصغر
- **الاستخدام**: للتجربة والاختبار
- **التوزيع**: داخلي فقط

### Production Build (للإنتاج)
- **النوع**: APK أو AAB
- **الحجم**: محسّن
- **الاستخدام**: للنشر على Google Play
- **التوزيع**: عام

---

## إعدادات البناء الحالية ⚙️

من ملف `eas.json`:

```json
{
  "build": {
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "android": {
        "buildType": "apk"
      }
    }
  }
}
```

---

## الأوامر المفيدة 🛠️

### عرض حالة البناء
```bash
eas build:list
```

### إلغاء البناء
```bash
eas build:cancel
```

### عرض سجل البناء
```bash
eas build:view [BUILD_ID]
```

---

## نصائح مهمة 💡

1. **أول بناء**: قد يستغرق 15-30 دقيقة
2. **البناءات التالية**: أسرع (5-15 دقيقة)
3. **الحد الأقصى**: 30 بناء مجاني شهرياً
4. **حجم APK**: حوالي 50-80 MB

---

## حل المشاكل الشائعة 🔧

### مشكلة: "eas: command not found"
```bash
npm install -g eas-cli
```

### مشكلة: "Not logged in"
```bash
eas login
```

### مشكلة: "Build failed"
- تحقق من ملف `app.json`
- تحقق من `package.json`
- راجع سجل الأخطاء

---

## الخطوات التالية بعد البناء 🚀

1. ✅ حمّل APK من رابط EAS
2. ✅ ثبّت على هاتف Android
3. ✅ اختبر جميع الميزات
4. ✅ تحقق من إصلاحات تقطيع النصوص
5. ✅ اختبر على أجهزة مختلفة

---

## روابط مفيدة 🔗

- [Expo EAS Build Docs](https://docs.expo.dev/build/introduction/)
- [Android Build Configuration](https://docs.expo.dev/build-reference/android-builds/)
- [Troubleshooting Guide](https://docs.expo.dev/build-reference/troubleshooting/)

---

**ملاحظة**: تم إصلاح جميع مشاكل تقطيع النصوص (27 ملف) وهي جاهزة في هذا البناء! ✅
